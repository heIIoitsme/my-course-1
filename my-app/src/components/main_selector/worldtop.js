import '../main_selector/main_selector.css';
import {largecard} from "../slots/slot";

export default () => {
    return (
        <div class='card_container'>
            {largecard}
        </div>
    )
}